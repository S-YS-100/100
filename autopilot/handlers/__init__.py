"""Handlers package - modules here register routes with the central router.

Each module exposes a `register(router)` coroutine or function which is invoked
by application startup when handlers are wired.
"""
# Empty file to mark package
