"""Services package - contains isolated service implementations.

Each service is a thin wrapper that encapsulates external integrations (APIs,
storage, caches) and provides async methods for the rest of the application.
"""
# package marker
